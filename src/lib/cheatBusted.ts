// =========================================================
// Гумористична реакція на відхилення сабміту сервером (тригер валідації
// historyу ladder_entries, supabase/migrations/0005). У ЧЕСНІЙ грі це
// НІКОЛИ не спрацьовує — клієнт сам рахує ті самі числа, що потім
// перевіряє тригер, тож розбіжність означає, що хтось підмінив дані
// вручну (консоль/мережа) перед відправкою.
// =========================================================

export interface BustedJoke {
  title: string;
  line: string;
}

const JOKES: Array<{ match: RegExp; title: string; line: string }> = [
  {
    match: /перехід .* неможлив/i,
    title: '🚨 ГРАВЕЦЬ З МАЙБУТНЬОГО',
    line: 'Один із переходів рівня порушує не лише шанси, а й арифметику. Сервер це помітив.',
  },
  {
    match: /luck_score/i,
    title: '🍀 ЗАНАДТО ЩАСЛИВИЙ, ЩОБ БУТИ ПРАВДОЮ',
    line: 'Luck Score не сходиться з реальними шансами таблиці — навіть із запасом на похибку.',
  },
  {
    match: /success_rate/i,
    title: '🧮 МАТЕМАТИКА ТЕБЕ НЕ ЛЮБИТЬ',
    line: '% успіху не б’ється з кількістю перемог у наданій історії спроб.',
  },
  {
    match: /довжина history/i,
    title: '✂️ ОБРІЗАНА ІСТОРІЯ',
    line: 'Кількість спроб не збігається з довжиною надісланого журналу спроб.',
  },
  {
    match: /не збігається з рівнем предмета/i,
    title: '🧵 ПЕРЕПЛУТАНІ ЛАНЦЮЖКИ',
    line: 'Історія предмета згадує рівень, якого він не мав. Сервер веде кожен предмет окремо.',
  },
  {
    match: /статистика .* не відповідає/i,
    title: '📊 ФЕЙКОВА СТАТИСТИКА',
    line: 'Стріки, падіння чи камбек не відповідають наданій історії спроб.',
  },
  {
    match: /фінальний рівень/i,
    title: '🎭 ІНШИЙ ФІНАЛ',
    line: 'Рівень наприкінці історії — не той, що прийшов у заявці.',
  },
];

const DEFAULT_JOKE: BustedJoke = {
  title: '🚨 СПІЙМАНО НА ГАРЯЧОМУ',
  line: 'Сервер помітив, що результат не сходиться з чесною грою.',
};

/** Чи це саме відхилення нашим тригером валідації (а не мережева/інша помилка). */
export function isValidationRejection(message: string): boolean {
  return message.includes('ladder_entries:');
}

/** Відхилення через ЛІМІТИ РЕСУРСІВ — НЕ звинувачення в читерстві: чесна
 * гра теж сюди потрапляє, якщо адмін змінив ліміти посеред забігу (запас
 * ×1.5 не безмежний) або забіг почато за старих правил. Показуємо
 * нейтральне пояснення замість "спіймано на гарячому". */
export function isLimitsRejection(message: string): boolean {
  return /понад ліміт|перевищує абсолютний ліміт/.test(message);
}

export function bustedJokeFor(message: string): BustedJoke {
  return JOKES.find((j) => j.match.test(message)) ?? DEFAULT_JOKE;
}
