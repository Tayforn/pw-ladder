-- Ладдер страждання — додає лічильник спроб для чесного рейтингу.
-- Виконати один раз: Dashboard → SQL Editor → New query → вставити
-- весь файл → Run.
--
-- Рейтинг тепер: спершу за найвищим рівнем заточки, а серед однакових
-- рівнів — за НАЙМЕНШОЮ кількістю спроб (бали легко накрутити нескінченним
-- кліканням міража, спроби так просто не підробиш).

alter table ladder_entries add column if not exists attempts int not null default 0;
